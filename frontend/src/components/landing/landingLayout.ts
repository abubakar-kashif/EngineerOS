/**
 * Shared layout tokens for the public landing page.
 * Keep section rhythm consistent so adjacent blocks never feel glued together.
 */
export const LANDING_CONTAINER =
  "mx-auto w-full max-w-[1200px] px-6 sm:px-8 lg:px-10";

/** Generous vertical breathing room between major page sections. */
export const LANDING_SECTION =
  "relative scroll-mt-28 py-20 sm:py-24 lg:py-32";
